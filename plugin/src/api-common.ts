/**
 * A symbol for the event emitter of a handle.
 */
export const emitter = Symbol("emitter");

/**
 * A symbol that refers to the function used to destroy a handle.
 */
export const destroy = Symbol("destroy");
